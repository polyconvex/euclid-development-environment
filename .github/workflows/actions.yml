on: push

jobs:
  ubuntu-tests:
    runs-on: ubuntu-latest
    steps:
      - shell: bash
      - uses: actions/checkout@v3
      - name: Run steps
        env:
          GITHUB_TOKEN: ${{ secrets.TOKEN }}
        run: |
          echo $GITHUB_TOKEN TTT