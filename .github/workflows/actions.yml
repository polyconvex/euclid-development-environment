on: push

jobs:
  ubuntu-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - shell: bash
      - name: Run steps
        env:
          GITHUB_TOKEN: ${{ secrets.TOKEN }}
        run: |
          echo $GITHUB_TOKEN TTT